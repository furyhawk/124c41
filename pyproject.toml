[project]
name = "124c41"
version = "0.1.0"
description = ""
authors = [{name = "furyhawk"}]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ipykernel>=6.29.2",
    "nbformat>=5.9.2",
    "black>=24.2.0",
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.6.9",
    "mkdocs-awesome-pages-plugin>=2.10.1",
]

[tool.setuptools.packages.find]
include = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
